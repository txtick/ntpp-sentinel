# Deployment Docs Moved

Deployment, startup, recovery, and verification instructions are now consolidated in:

- `docs/START UP & RECOVERY.md`

Use that file as the single operations runbook.

If you configure the optional Skimmer nightly dump import flow, the required env vars are documented in `docs/START UP & RECOVERY.md`.
